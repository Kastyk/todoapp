import { GetLogoIdDirective } from './get-logo-id.directive';

describe('GetLogoIdDirective', () => {
	it('should create an instance', () => {
		const directive = new GetLogoIdDirective();
		expect(directive).toBeTruthy();
	});
});
